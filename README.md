# initdots

Personal dotfiles for a noctalia/Hyprland desktop on Arch/CachyOS.

**Stack**: Hyprland (Lua config) · Noctalia v5 · Ghostty · fish · tmux · fastfetch 

**Setup**:

```
git clone https://codeberg.org/initsyscall/initdots.git && cd initdots && stow -t $HOME --ignore='README.md' .
```
